# Adapter Documentation

Adapters document provider-specific installation behavior. They do not contain separate skill implementations.

Canonical skill content lives in `skills/`.

Read `config/providers.json` for exact ids, paths, detection probes, and bridge destinations.
